---
name: ck-scope
description: "Effort estimation derived from the build site — dimensions, routes, AI-adjusted estimates"
argument-hint: "<topic> [--wiki]"
---

# Cavekit Scope — Effort Estimation

Analyze the build site and kits to produce an agent-optimized effort estimate. Structured around **dimensions** (independent axes of work) with implementation routes ranging from minimal to comprehensive. Includes AI-adjusted estimates.

This is a **post-map** step — it reads the build site generated by `/ck:map` for maximum accuracy.

## Workspace root resolution

Before any substantive work, attempt to resolve the **workspace root**:

1. Start from the current working directory.
2. Walk upward through parent directories until you find one that contains all of these markers:
   - A `sanetics-wiki/` directory
   - A `setup.sh` file
   - A `.claude/` directory
3. If such an ancestor is found, remember its absolute path as `WORKSPACE_ROOT`. From the workspace root the wiki directory lives at `<WORKSPACE_ROOT>/sanetics-wiki/` and each sub-repo is a sibling (for example `<WORKSPACE_ROOT>/saneticscode/`, `<WORKSPACE_ROOT>/saniloop-aggregate/saniloop/`, `<WORKSPACE_ROOT>/claude-code/cavekit/`).
4. If no ancestor contains all three markers, `WORKSPACE_ROOT` is **unset** — the command was invoked outside a sanetics-workspace. Fall back to plain CWD behavior (no workspace-aware features).

Do not use environment variables or hardcoded absolute paths for workspace resolution. The workspace root is always discovered dynamically from the current working directory.

## Context selection

If `WORKSPACE_ROOT` is set, determine the **target sub-repo** before Step 1:

1. **Detect invocation location:** compare CWD to `WORKSPACE_ROOT`.
   - If CWD is inside a sub-repo (CWD is a descendant of `WORKSPACE_ROOT`), the enclosing sub-repo is the target automatically — do not prompt.
   - If CWD equals `WORKSPACE_ROOT`, you are at the workspace level (handled in step 2).
2. **Workspace-root invocation:** when CWD is the workspace root, check whether a target sub-repo was passed as an argument.
   - If a sub-repo name was passed (e.g. `saneticscode`, `saniloop-aggregate/saniloop`), resolve it as `<WORKSPACE_ROOT>/<name>` and use it as the target.
   - If no sub-repo argument was passed, list the sibling sub-repos under `WORKSPACE_ROOT` and prompt the user to pick one.
   - After selection, operate as if invoked from within the chosen sub-repo — read its codebase and read/write its `context/` directory.
3. **Cross-reference another sub-repo:** accept an optional reference to another sub-repo whose context should also be read. The reference is given by directory name relative to `WORKSPACE_ROOT`. When provided, resolve it as `<WORKSPACE_ROOT>/<reference>` and read `context/kits/`, `context/plans/`, `context/impl/` from **both** the target sub-repo and the referenced sub-repo.

If `WORKSPACE_ROOT` is unset, skip this section: the target is simply the CWD.

## Step 1: Wiki Routing

Parse `$ARGUMENTS` for the `--wiki` flag.

- **If `--wiki` is NOT present** → **local mode**. Use `context/` paths under the target sub-repo (or CWD if `WORKSPACE_ROOT` is unset). Do not read any wiki routing document.
- **If `--wiki` IS present** → read and follow the wiki routing document. Do not inline any routing procedure details here — the document is the source of truth for project discovery, PR sync, Grav front matter, and auto-commit.
  - If `WORKSPACE_ROOT` is set, prefer `<WORKSPACE_ROOT>/.claude/wiki-routing.md` (accessible from any sub-repo via the `.claude` symlink as `.claude/wiki-routing.md`).
  - Otherwise, fall back to `"${CLAUDE_PLUGIN_ROOT}/references/wiki-routing.md"`.

## Step 2: Read Inputs

1. **Build site:** read from wiki `11.build-site/` (wiki mode) or `context/plans/` (local mode). If no build site exists, tell the user to run `/ck:map` first.
2. **Kits:** read from wiki `10.kits/` (wiki mode) or `context/kits/` (local mode)
3. **Related docs** (wiki mode): read existing specs and overviews in the project folder
4. **Research AI productivity multiplier:** web search for current studies on AI-assisted coding productivity (GitHub, McKinsey, Stanford, etc.)

## Step 3: Interview

Before producing the document, ask targeted questions if critical context is missing:
- Scope boundaries or constraints not covered by kits
- Business priorities affecting route selection
- Timeline constraints

Do not proceed with significant unknowns.

## Step 4: Write Scope

Write the scope in **kit-style format** — terse, tables, Mermaid diagrams.

### Required sections (in order):

1. **Summary** — brief overview + two sub-tables:

   **Dimensions at a Glance:**

   | | D1 Name | D2 Name | D3 Name |
   |---|:---:|:---:|:---:|
   | **Skippable?** | No | No | **Yes** |
   | **Type** | Review | Design | Refactoring |
   | **Complexity** | High | Medium | High |
   | **Risk** | High | Medium | High |
   | **Confidence** | Low | Medium | Low |
   | **Depends on** | -- | D1 | D2 |
   | **Human exp.** | **22h (3d)** | **8h (1d)** | **16h (2d)** |
   | **AI-assisted exp.** | **20h (2.5d)** | **7h (1d)** | **12h (1.5d)** |
   | Route 1 (Minimal) | Incl. | Incl. | -- |
   | Route 2 (Comprehensive) | Incl. | Incl. | Incl. |

   **Route totals:**

   | Route | Human | AI-assisted | Delta |
   |-------|-------|-------------|-------|
   | Route 1: Minimal | **94h (12d)** | **71h (9d)** | -23h |
   | Route 2: Comprehensive | **110h (14d)** | **83h (10.5d)** | -27h |

2. **Dimensions overview** — Mermaid diagram showing all dimensions and relationships

3. **Per-dimension analysis** (repeat for each):
   - What it is (1 line)
   - Subtask table: #, Subtask, Required/Optional, Complexity, Effort, Dependencies
   - Complexity rating: lines of code, files touched, risk, overall
   - Effort estimate table: Optimistic/Expected/Pessimistic × Human/AI-assisted, with multiplier and confidence

4. **Implementation routes** — each with:
   - Mermaid flowchart showing the sequence
   - Summary table: total effort, risk, trade-offs, when to choose

5. **Dependency map** — Mermaid diagram of all subtasks and dependencies

6. **Open questions** — table: #, Question, Affects (route/dimension), Priority

### AI-adjusted estimates

Every effort estimate includes two perspectives:

| Task type | AI multiplier | Meaning |
|---|---|---|
| Boilerplate, scaffolding, test generation | 2.0-3.0x | AI takes 33-50% of human time |
| Standard features, refactoring, bug fixes | 1.3-1.5x | AI takes 67-77% of human time |
| Architecture, design, investigation | 1.0-1.2x | Minimal AI benefit |
| Complex integration, novel problems | 1.0-1.3x | Modest AI benefit |

Include multiplier in parentheses. Cite research sources.

### Front matter

**Wiki mode:**
```yaml
---
title: '<Topic> (Scope)'
template: doc
taxonomy:
    product: [<product from project chapter.md>]
    category: [project]
published: true
visible: true
created: "<ISO 8601 UTC>"
last_edited: "<ISO 8601 UTC>"
---
```

**Local mode:**
```yaml
---
created: "<ISO 8601 UTC>"
last_edited: "<ISO 8601 UTC>"
---
```

## Step 5: Write Path

**Wiki mode:**
- Create subfolder: `<wiki-project>/03.YYYY-MM-DD-HHmm-scope-<topic-slug>/`
- Write file inside: `scope-<topic-slug>.md`
- If subfolder already exists, append `-2`, `-3`, etc.

**Local mode:**
- Write to `context/specs/YYYY-MM-DD-scope-<topic-slug>.md`
- Create directory if needed

## Step 6: Finalize

1. Print the file path on completion
2. **Auto-commit** (wiki mode only): follow auto-commit procedure in wiki routing reference section 5

## What a scope IS

- Effort estimation tool — bridge between "what do we want?" and "how long will it take?"
- Decision-support: presents routes with trade-offs, not a single recommendation
- Derived from concrete build site tasks for accuracy

## What a scope is NOT

- Not a technical plan — that's `/ck:sketch` → `/ck:map`
- Not a feature specification — use `/ck:spec`
- Not a codebase overview — use `/ck:overview`

## Topic

$ARGUMENTS
